version: '3'
services:
  app:
    container_name: express-ts-api
    restart: always
    build: .
    ports:
      - '80:1234'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
